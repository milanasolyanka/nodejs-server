version: "3.9"

services:
  profiles:
    build: ./profiles
    ports:
      - "3001:3001"
    networks:
      - mynetwork
    deploy:
      placement:
        constraints:
          - node.hostname == UbuntuProfiles

  about:
    build: ./about
    ports:
      - "3002:3002"
    networks:
      - mynetwork
    deploy:
      placement:
        constraints:
          - node.hostname == UbuntuAbout

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    networks:
      - mynetwork
    deploy:
      placement:
        constraints:
          - node.hostname == UbuntuManager

networks:
  mynetwork:
    external: true
