input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

output {
  file {
    path => "/usr/share/logstash/logs/all_services.log"
    codec => line { format => "%{message}" }
  }
  stdout { codec => rubydebug }
}
